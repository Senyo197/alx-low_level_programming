#include "main.h"
#include <stdlib.h>
/**
*_abs - determines the absolute value of a number
*
*@n: argument to check code
*
*Return: absolute value.
*/

int _abs(int n)
{
int absn = abs(n);
return (absn);
}
